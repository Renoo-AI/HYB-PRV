<%- include(typeof isolated !== 'undefined' && isolated ? 'minimal_layout' : 'layout', { title: 'Level 5: Hidden Page Guessing', levelNumber: 5, body: `
    <div class="challenge-box">
        <h2>Can you guess the admin page?</h2>
        <p>A site has a hidden admin page that isn't linked anywhere. What common path could it be?</p>
    </div>
` }) %>
