<%- include('layout', { title: 'Level 11: JS Password Leak', levelNumber: 11, body: `
    <div class="challenge-box">
        <h2>Read the source code</h2>
        <form method="POST" action="/level/11/login">
            <input type="password" name="password" id="js-password" placeholder="Password" required>
            <button type="submit" class="btn btn-primary">Login</button>
        </form>
        <% if (error) { %>
            <p class="error-msg"><%= error %></p>
        <% } %>
        <script src="/js/level11.js"></script>
    </div>
` }) %>
