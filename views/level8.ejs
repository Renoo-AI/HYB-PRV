<%- include(typeof isolated !== 'undefined' && isolated ? 'minimal_layout' : 'layout', { title: 'Level 8: Price Manipulation', levelNumber: 8, body: `
    <div class="challenge-box">
        <h2>Buy the flag!</h2>
        <p>The flag costs $100. Current price: <strong>$${price}</strong></p>
        <button class="btn btn-primary">Buy</button>
        <p>Can you change the price parameter to 1?</p>
    </div>
` }) %>
