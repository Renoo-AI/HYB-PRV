<%- include(typeof isolated !== 'undefined' && isolated ? 'minimal_layout' : 'layout', { title: 'Level 10: Base64 Cookie Decode', levelNumber: 10, body: `
    <div class="challenge-box">
        <h2>Decode and edit!</h2>
        <p>Your auth cookie value: <strong>${auth}</strong></p>
        <p>It looks like base64. Decode it, change the role to admin, encode it back, and update your cookie.</p>
    </div>
` }) %>
